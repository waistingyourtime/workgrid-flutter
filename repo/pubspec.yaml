name: workgrid
description: Прототип приложения для управления проектами и рабочими объектами
publish_to: 'none' # приватный проект, не публикуем

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # стандартные пакеты
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
